# 配置数据库连接信息
data_source:
  host: "localhost"
  port: 3306
  user: "root"
  password: "password"
  database: "nest-code-generate"

# 配置字段收取及基础类(可选，如果没有这个配置就和之前一样的)
data_config:
  collect: "id, created_at, updated_at, deleted_at"
  base_name: "BaseEntity"
